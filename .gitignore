# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.mjs)
media/img/
media/gif/
frontend/public/media/

# Runtime data — users, passkeys, per-user state, session secret
data/

# Local config
.env

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store
node_modules
