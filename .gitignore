# Exercise media (~140 MB) — fetched locally via `npm run media:fetch`, never committed
frontend/public/media/

# Runtime data — users, passkeys, per-user state, session secret
data/

# Local config
.env

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store
